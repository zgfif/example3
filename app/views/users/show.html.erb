<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Surname:</strong>
  <%= @user.surname %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @user.phone %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Balance:</strong>
  <%= @user.balance %>
</p>
<p>
  <strong><%= @user.name %> added</strong>
  <%= @user.added %> books
</p>
<p>
	Added by <%= @user.name %>:<br>
	<% @user.books.each do |book| %>
		
		|<%= link_to book.title, book_path(book) %>|  
	<% end %> 
</p>
<p>
	Hold by <%= @user.name %>:<br>
	<%# @holder.books.each do |book| %>
		
	<%#= link_to book.title, book_path(book) %>|
	<%# end %> 
</p>

<%= link_to 'Edit profile', edit_admin_user_path(@user) %> |
<%= link_to 'Back', users_path %>
